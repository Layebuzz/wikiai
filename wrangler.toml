# Cloudflare Workers configuration for WikiAI v2
# Deploy target: https://wikiai.layebuzz.workers.dev
name = "wikiai"
compatibility_date = "2025-08-01"

# Static assets only — no build step required.
[assets]
directory = "./"
not_found_handling = "404-page"

# Uncomment to run the daily catalog crawler (see cron-worker/README.md):
# [[triggers]]
# crons = ["0 5 * * *"]